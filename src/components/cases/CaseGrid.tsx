import { useState } from "react";import { motion } from "framer-motion";import CaseCard from "./CaseCard";const cases=[{id:1,title:"大型钢铁企业数字孪生项目",category:"数字孪生",description:"为某大型钢铁企业打造全流程数字孪生系统",image:"/images/case-1.jpg",videoUrl:"https://example.com/case-1-video"},{id:2,title:"汽车制造工艺仿真优化",category:"工艺仿真",description:"通过仿真技术优化汽车制造工艺流程",image:"/images/case-2.jpg",videoUrl:"https://example.com/case-2-video"}];const categories=["全部","数字孪生","工艺仿真","三维展示","动态模拟"];export default function CaseGrid(){const[selectedCategory,setSelectedCategory]=useState("全部");const[searchQuery,setSearchQuery]=useState("");const filteredCases=cases.filter(case_=>{const matchesCategory=selectedCategory==="全部"||case_.category===selectedCategory;const matchesSearch=case_.title.toLowerCase().includes(searchQuery.toLowerCase())||case_.description.toLowerCase().includes(searchQuery.toLowerCase());return matchesCategory&&matchesSearch;});return(<section className="py-20"><div className="container mx-auto px-4"><motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} className="text-center mb-16"><h1 className="text-4xl font-bold text-gray-900 mb-4">经典案例</h1><div className="w-20 h-1 bg-primary mx-auto"></div></motion.div><div className="mb-8 space-y-6"><div className="max-w-md mx-auto"><input type="text" placeholder="搜索案例..." className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary" value={searchQuery} onChange={(e)=>setSearchQuery(e.target.value)}/></div><div className="flex flex-wrap justify-center gap-4">{categories.map((category)=>(<button key={category} className={`px-4 py-2 rounded-full transition-colors ${selectedCategory===category?"bg-primary text-white":"bg-gray-100 text-gray-700 hover:bg-gray-200"}`} onClick={()=>setSelectedCategory(category)}>{category}</button>))}</div></div><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">{filteredCases.map((case_,index)=>(<motion.div key={case_.id} initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{delay:index*0.1}}><CaseCard {...case_}/></motion.div>))}</div></div></section>);}
